{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main NozyWallet window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
