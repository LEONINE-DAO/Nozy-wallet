# Site assets

| File | Used by |
|------|---------|
| `logo.png` | GitHub Pages (`_site/logo.png`), desktop in-app UI (`desktop-client/public/logo.png`), Tauri bundle icons (`scripts/generate-desktop-icons.ps1`) |

The landing dev server reads `landing/public/logo.png` (same image).
